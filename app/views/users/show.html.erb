<div class="clearfix top-space small"></div>
<div class="row">

  <%= render :partial => 'shared/left_nav' %>

  <div class="panel panel-default col-md-8 text-left">
    <div class="pull-left">
      <h4>Tweets</h4>
    </div>
    <div class="clearfix"></div>
    <hr />
    <ol class="list-unstyled">
      <% @twets.each do |twet| -%>
      <li>
       	<%= link_to twet.user.name, user_path(twet.user.id), :content_tag => :small, :class => 'pull-left text-middle' %>
        <%= link_to "@"+twet.user.username, user_path(twet.user.id), :content_tag => :small, :class => 'text-muted pad-10 text-middle' %>
        <%= content_tag :small, twet.created_at.strftime("%b %-d"), :class => 'text-muted text-middle pull-right' %>
        <div class="clearfix"></div>
        <%= content_tag :p, twet.content %>
      </li>
      <li><hr></li>
      <% end -%>
    </ol>
  </div>
</div>